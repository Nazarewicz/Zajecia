@model IEnumerable<AppDziennik.Models.Ocena>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id_u)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Wartosc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Wartosc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_o }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_o }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_o })
        </td>
    </tr>
}

</table>
